import React, {useState} from "react";

const DetailPage = () => {
    const [link, setLink] = useState(null);

    return (
        <div>
            <h1>DetailPage</h1>
        </div>
    );
}

export default DetailPage;
